---
- name: Git clone GitHub Repos
  git:
    repo: "git@github.com:wrightbradley/{{ item }}.git"
    dest: "~/Projects/code/{{ item }}"
    version: HEAD
    accept_hostkey: true
  loop:
    - dotfiles
    - nvim
